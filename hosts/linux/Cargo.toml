[package]
name = "operit-host-linux-native"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
operit-host-api = { path = "../../core/crates/operit-host-api" }
regex = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
